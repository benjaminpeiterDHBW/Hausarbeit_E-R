% \hypersetup{
%   pdftitle={\deliverytitle{} Docu},                     % Titel
%   pdfauthor={\deliveryauthor{} \deliveryemail{}},     	% Verfasser
%   pdfsubject={\headerdescription{} (\deliverytitle)},   % Thema
%   pdfkeywords={ZF} {BrakeSystem} {IBC}
%     {Documentation}
%     {\deliverytitle},                                   % Stichwoerter
%   pdfcreator={\LaTeX{} Docu Template},                  % Anwendung
%   pdfproducer={pdflatex},                               % PDF erstellt mit
%   pdfmenubar=true,                                      % Show Menubar
%   pdftoolbar=false,                                     % Show Toolbar
%   pdfdisplaydoctitle=true,                              % Show document title instead of file name
%   pdffitwindow=false,                                   % Fit to windows
%   pdfstartview={FitBH},
%     % Fit                               Fits the page to the window.
%     % FitH    top                       Fits the width of the page to the window.
%     % FitV    left                      Fits the height of the page to the window.
%     % FitR    left bottom right top     Fits the rectangle specified by the four coordinates to the window.
%     % FitB                              Fits the page bounding box to the window.
%     % FitBH   top                       Fits the width of the page bounding box to the window.
%     % FitBV   left                      Fits the height of the page bounding box to the window.
% }

\pdfsuppressptexinfo=1
% \pdfinfo{/DeliveryTitle (\deliverytitle)}
% \pdfinfo{/DeliveryDate (\deliverydate)}
% \pdfinfo{/DeliveryNumber (\deliverynumber)}